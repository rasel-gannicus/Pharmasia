import Wishlist from '@/components/User Dashboard/Wishlist/Wishlist';
import React from 'react';

const page = () => {
    return (
        <div>
            <Wishlist />
        </div>
    );
};

export default page;